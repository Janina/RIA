function Dialog(text, e) //Konstruktorfunktion
{
  this.title = e.getDate();
  this.text = e.getText();
   
   /*
    * skapar och skriver ut dialogen(meddelande-rutan). jQuery.
    */
   function printDialog(e)
   {
     
   }
   
   
}
